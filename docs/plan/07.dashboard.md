# 07 — Web Dashboard

> **Status: v1 shipped in the v0.2 milestone.** Delivered: login,
> apps list, new-app wizard, app detail, live logs (SSE), env editor,
> domains manager, and a settings tab. Deferred to a later milestone:
> the terminal screen, team/audit/tokens screens, CPU/mem stat
> sparklines, i18n, and a Playwright smoke test — coverage for the
> deferred Playwright flow is instead provided by
> [`scripts/e2e-local.sh`](../../scripts/e2e-local.sh)'s CI-run
> assertions against the real dashboard shell/assets, the logs SSE
> stream, and the rendered Caddy domain config (see doc 11).

## Stack

- **Vue 3 + TypeScript + Vite**, `web/` directory.
- **Nuxt UI v3** as the component library (it supports plain Vue apps via
  its Vite module — no Nuxt framework needed). If it proves awkward
  outside Nuxt in practice, fallback is **PrimeVue** — decide by building
  the login + app-list screens first, before committing further.
- Pinia for state, Vue Router, TanStack Query (vue-query) for API
  caching/refetching.
- Charts: lightweight (uPlot or chart.js) for CPU/mem sparklines.
- Build output embedded into the Go binary via `go:embed` of `web/dist`;
  the daemon serves it with SPA fallback. `basepod server --dev` proxies
  to the Vite dev server instead, for hot reload during development.

No separate frontend deployment, no Node.js on servers — Node is a
build-time dependency only (CI builds `web/dist` before `go build`).

## Design intent

Dense-but-calm ops dashboard: sidebar navigation (Apps, Templates,
Team, Servers, Plugins, Settings), top bar with team switcher + user
menu. Dark mode from day one (Nuxt UI supports it natively). Live data
everywhere it matters — status changes arrive over the `/events` SSE
stream, no manual refresh.

## Screens (v1)

| Screen | Contents |
|---|---|
| **Login / Setup** | First-run wizard (admin account, root domain) when `/setup/status` says fresh; login otherwise |
| **Apps list** | Cards/table: name, status dot, domains, last deploy, CPU/mem sparkline; "New app" flow |
| **App → Overview** | Status, instances, image, internal hostname, quick actions (restart, stop, rollback), resource graphs |
| **App → Deployments** | History with trigger/user/git SHA, live build log (SSE) for the running one, rollback buttons |
| **App → Logs** | Live tail (SSE), pause/scrollback, tail-size selector, download |
| **App → Environment** | Env editor (bulk paste `.env` supported), secrets masked write-only, "changes deploy on save" notice |
| **App → Domains** | Generated + custom domains, DNS instructions with live check, TLS status per domain, redirect/basic-auth toggles |
| **App → Settings** | Port, healthcheck, resources, deploy strategy, replicas, volumes, danger zone (delete w/ typed confirm) |
| **App → Terminal** | xterm.js over the `/exec` WebSocket (role-gated) |
| **New app wizard** | Source picker: image / upload / git / compose / template (from doc 09 catalog) |
| **Team** | Members + roles, invitations, per-member audit trail link |
| **Audit log** | Filterable table (who, what, when, from where) |
| **Tokens** | PAT create/revoke (shown once) |
| **Settings** | Root domain, ACME email, registries, backups, upgrade check |
| **Plugins** | Doc 09: installed list, enable/disable, config forms, catalog |
| **Servers** | Doc 10 (phase 7): node list, health, app placement |

## Frontend architecture notes

- Generated (or hand-kept, per doc 05 decision) TypeScript API client
  from the OpenAPI spec — one source of truth, compile-time drift check.
- SSE wrapper composable (`useEventStream`) with auto-reconnect/backoff
  shared by logs, build logs, stats, events.
- Route-level code splitting; the whole dashboard should stay light —
  budget: < 500 KB gzipped initial load.
- All state changes optimistic-with-rollback only where safe (env editor
  no, restart button yes).
- i18n scaffolding (vue-i18n) from the start, English-only content in v1.

## Testing

- Vitest component tests for critical logic (env editor parsing, SSE
  composable, RBAC-conditional rendering).
- One Playwright smoke flow in CI against a real dev server: login →
  create app from image → see it running → view logs (doc 11).

## Feature checklist

- [ ] Vite + Vue 3 + Nuxt UI scaffold; embed pipeline into Go binary
- [ ] Nuxt-UI-vs-PrimeVue spike (login + apps list) and final call
- [ ] Auth flow, first-run setup wizard
- [ ] Apps CRUD + new-app wizard (image + upload paths first)
- [ ] Live logs, build logs, stats, events (SSE composable)
- [ ] Env / domains / settings editors
- [ ] Terminal (xterm.js)
- [ ] Team, tokens, audit screens
- [ ] Dark mode, responsive down to tablet; mobile = read-only friendly
- [ ] Playwright smoke test wired into CI
