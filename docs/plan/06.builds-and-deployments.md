# 06 — Builds, Deployments & CLI

> **Status (v0.3):** delivered — tarball-upload build pipeline (gzip
> decompress, tar validate, podman build), build-log endpoint (live SSE
> while building, plain text once finished), zero-downtime rollout (this
> doc's default strategy), rollback to any earlier healthy deployment,
> built-image retention, and the CLI core (`login`, `apps`, `deploy`
> from source or `--image`, `logs`, `env`, `rollback`, `status`,
> `context`). Exercised end-to-end in
> [`scripts/e2e-local.sh`](../../scripts/e2e-local.sh), run in CI.
> **Status (v0.4, in progress):** `basepod.yaml` manifest parsing
> (`internal/manifest`) and `basepod init` (scaffolds a manifest plus a
> starter Containerfile, detecting Node/Go/Python/static/generic
> projects) are delivered — see the "App manifest" section below for the
> exact v1 field subset. The build pipeline
> (`internal/build.Builder.BuildManifest`) detects and parses a
> root-level `basepod.yaml`/`basepod.yml` in the same pass as its
> existing tar validation, surfacing parse warnings directly in the
> build log; `basepod deploy` reads a local manifest to default `--app`.
> **Not yet wired**: the parsed manifest's `port`/`healthcheck.path`/
> `resources`/`env` are not yet applied onto the app record by
> `internal/deploy` — `Builder.Build` (unchanged 3-return signature)
> remains what `internal/deploy` calls today; switching it to
> `BuildManifest` and applying those defaults is a small follow-up (see
> the `// TODO(#1)` comment beside `BuildManifest` in
> `internal/build/build.go`). No JSON schema is published yet.
> **Deferred** (still as designed below, not yet built): git webhooks,
> compose support, buildpack auto-detection, and the `strategy: replace`
> opt-in and same-host replicas.

## Deploy sources

| Source | How it arrives | Build step? |
|---|---|---|
| **Image** | UI/API/CLI provides `ghcr.io/user/app:tag` | No — pull only |
| **CLI tarball** | `basepod deploy` tars cwd → `POST /deploy/tarball` | Yes |
| **Git** | Webhook (push event) or manual "deploy branch" button | Yes — server clones |
| **Compose** | `compose.yaml` via UI/CLI | Per-service (build or pull) |

## App manifest — `basepod.yaml` (optional, in repo root)

```yaml
# all fields optional; flags/UI override
name: blog
port: 8080                 # container port to expose
healthcheck:
  path: /healthz           # default: TCP connect on port
build:
  containerfile: ./Containerfile   # default: auto-detect
  context: .
  args: {NODE_ENV: production}
env:                       # non-secret defaults; secrets set via UI/CLI
  TZ: Europe/Tirane
volumes:
  - data:/var/lib/app
resources:
  memory: 512m
  cpus: 1.0
```

Equivalent of CapRover's `captain-definition`, but YAML and optional:
zero-config deploys must work.

## Build pipeline

States: `queued → building → deploying → healthy | failed`.

1. **Acquire source** — untar upload / `git clone --depth 1 -b <branch>`
   (deploy keys or https+token from encrypted registry/creds store).
2. **Resolve build strategy**:
   - `Containerfile`/`Dockerfile` present (or set in manifest) → podman
     build via bindings, build args from manifest.
   - No Containerfile → **v1**: fail with a helpful message + template
     suggestions ("detected Node.js — here's a starter Containerfile").
     **v1.x**: buildpack auto-detection (nixpacks-style) as a builtin
     plugin (doc 09) — keeps core small.
3. **Tag** `localhost/basepod/<app>:<deploymentN>`.
4. **Deploy** (below). Build log captured to
   `<data_dir>/apps/<app>/builds/<n>.log`, streamed live via SSE.

Build concurrency: one build per app at a time (later deploy of same app
supersedes queued ones); global cap (default 2) to protect small VPSes.
Timeout default 60 min, as delivered (`internal/api/apps.go`) — this doc
originally scoped 15 min; the shipped value is longer.

## Zero-downtime deploy (default strategy)

1. Create new container `bp-<app>-<n>` (new Quadlet unit on Linux)
   alongside the old one — same network, same volumes.
2. Wait for health (doc 03) with deadline (default 60 s).
3. Healthy → Caddy manager swaps the route upstream to the new
   container's address (atomic full-config load, doc 04). **As designed**:
   old container gets 30 s drain before being stopped and removed. **As
   delivered in v0.3**: no drain — the old container is stopped and
   removed (unit removed too) immediately after the route swap.
4. Unhealthy → new container destroyed, deployment marked `failed`, old
   keeps serving. **A failed deploy never takes the app down.**

Caveats **as designed, not delivered** (see the feature checklist
below): apps holding exclusive volume locks (e.g. SQLite apps) would
opt into `strategy: replace` (stop-then-start, brief downtime) in the
manifest, and `instances > 1` (same-host replicas behind Caddy
load-balancing) would be supported for stateless apps, rolling one at a
time. Neither exists in v0.3 — every deploy is always the single-replica
zero-downtime cutover above; there is no `strategy` or `instances`
field anywhere (there is no `basepod.yaml` manifest parsing at all yet).

## Rollback

`basepod rollback <app> [n]` / UI button: re-deploy a retained image
(last 5 kept, doc 03) through the same zero-downtime path.

**As designed** (not yet built — see the checklist below): a
per-deployment env snapshot, so rollback restores config + code together.
**As delivered in v0.3**: rollback re-deploys the target image using the
app's *current* env config, not a historical snapshot — deployments don't
store one.

## Git integration (phase 5)

- Per-app webhook URL `POST /webhooks/git/<app-uuid>/<secret>` with
  HMAC verification (GitHub `X-Hub-Signature-256`, GitLab token, Gitea) —
  branch filter from app config.
- v1 is provider-agnostic generic webhooks (works with any host).
  Deeper GitHub App integration (commit statuses, PR preview apps) is a
  plugin candidate (doc 09).

## Compose support (phase 5)

- Accept a `compose.yaml`; translate the supported subset (services,
  image/build, env, volumes, depends_on, expose) into one BasePod app
  per service, linked by the shared network, DNS names preserved via
  network aliases matching service names.
- Unsupported keys → explicit warnings, never silent drops.
- Implementation candidates: `podman kube play` via generated K8s YAML,
  or direct translation to our own model. **Decision: direct
  translation** — keeps everything (domains, env UI, rollbacks) working
  per-service; kube play would bypass our model.

## CLI

Same binary, client mode; config at `~/.config/basepod/cli.yaml`
(contexts: multiple servers, current context, PAT).

```
basepod login https://basepod.example.com      # opens browser or prompts for PAT
basepod context list|use <name>

basepod apps [ls|create|delete|info <app>]
basepod init [--force] [--port N] [--name x]    # scaffold basepod.yaml + a starter Containerfile
basepod deploy [-a <app>] [--image ref] [path]  # default: tar cwd, respect .basepodignore
                                                 # --app defaults from basepod.yaml's `name` if present
basepod logs <app> [-f] [--tail 200]
basepod env <app> [set K=V ... | unset K | pull | push .env]
basepod domains <app> [add|rm <hostname>]
basepod rollback <app> [n]
basepod exec <app> [--] <cmd>       # interactive over WebSocket
basepod ps / status                 # server overview
basepod server / setup / upgrade / backup / uninstall   # host-side commands
```

UX details: `deploy` streams build + deploy progress with clear phases
and exits non-zero on failure (CI-friendly); `--json` on read commands
for scripting; helpful error when the daemon is unreachable.

## Feature checklist

- [x] Tarball upload deploy (end-to-end walking skeleton first)
- [x] Image-ref deploy incl. private registries — image-ref deploy delivered
      since v0.1; the "private registries" part isn't: `PullImage` takes no
      credentials, so only public refs (or refs the podman machine already
      has cached auth for) actually work.
- [x] basepod.yaml parsing + validation (`internal/manifest`, detected
      and parsed from the build context's tar root by
      `internal/build.Builder.BuildManifest`) — [ ] JSON schema not yet
      published; [ ] the parsed manifest's fields are not yet applied
      onto the app record by `internal/deploy` (see the status note
      above)
- [x] Build queue, logs, timeouts — global (default 2) + per-app
      concurrency limiting, SSE + persisted build logs, 60 min build
      timeout (not 15 min as stated above — see `internal/api/apps.go`).
- [ ] Build cancellation — not implemented; a queued or running build can
      only be stopped by the caller's connection dropping, no cancel
      endpoint or CLI verb exists.
- [x] Zero-downtime swap + failure rollback — new container created
      alongside the old one, health-checked, route swapped only after
      health passes; a failed deploy never touches the old container.
- [ ] 30 s drain before old-container removal — not implemented; the old
      container is stopped immediately after the route swap, with no
      grace period.
- [ ] `replace` strategy opt-in
- [ ] Replicas (same host) + rolling restart
- [x] Rollback — redeploys a retained image through the same
      zero-downtime path. **Not** "with env snapshots" as originally
      scoped below: rollback uses the app's *current* env config, since
      deployments don't store a historical per-deployment env snapshot.
- [ ] Generic git webhooks + HMAC
- [ ] Compose translation (documented subset)
- [x] CLI commands: `login`, `context`, `apps`, `init`, `deploy`, `logs`,
      `env`, `rollback`, `status`, `server`, `setup` (with context
      support).
- [ ] CLI subcommands not yet implemented: `domains`, `exec`, `ps`,
      `upgrade`, `backup`, `uninstall` (`domains` exists only as an HTTP
      API today, with no CLI wrapper).
