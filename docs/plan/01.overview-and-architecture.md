# 01 — Overview & Architecture

## Vision

BasePod is an open-source, self-hosted PaaS: the CapRover experience —
"push code, get a running app with HTTPS" — rebuilt on a modern,
daemonless, rootless stack:

- **Podman** instead of Docker/Swarm — rootless containers, no privileged
  daemon, systemd-native supervision via Quadlet.
- **Caddy** instead of nginx — automatic HTTPS, zero-downtime config
  reloads through a JSON admin API, on-demand TLS for customer domains.
- **One Go binary** instead of a stack of containers — installs on macOS
  (Intel & Apple Silicon) and any Linux VPS in one command.

## Comparison to prior art

| | CapRover | Coolify | Dokku | **BasePod** |
|---|---|---|---|---|
| Runtime | Docker Swarm | Docker | Docker | Podman (rootless) |
| Proxy | nginx | Traefik/Caddy | nginx | Caddy (admin API) |
| Control plane | Node.js in container | PHP/Laravel | shell + plugins | Single Go binary |
| macOS server support | no | no | no | yes (dev/homelab) |
| Teams/RBAC | no | yes | no | yes |
| Plugins | one-click apps only | limited | strong | hooks + OCI plugins + templates |

## Goals

1. **One-command install** on macOS (both architectures) and any Linux VPS.
2. **Deploy anything**: an image name, a directory with a Containerfile, a
   git repository, or a compose file.
3. **Automatic HTTPS** for `<app>.<root-domain>` and custom domains.
4. **Team management**: users, teams, roles, API tokens, audit log.
5. **Server management**: single server first; multiple servers behind one
   control plane later (designed for from day one).
6. **Extensible**: lifecycle hooks, container-based plugins, one-click app
   templates.
7. **No mandatory external dependencies**: SQLite embedded, Caddy and
   Podman are the only system requirements.

## Non-goals (v1)

- Kubernetes compatibility or migration tooling.
- Windows hosts (WSL2 may work; untested, unsupported).
- Autoscaling, multi-region scheduling, service mesh.
- Being a CI system — we build images, we don't run arbitrary pipelines
  (plugins can integrate external CI).

## High-level architecture

```
                        ┌─────────────────────────────── VPS / Mac ─┐
                        │                                           │
  developer ── CLI ──►  │  ┌──────────── basepod server ─────────┐  │
  browser ── HTTPS ──►  │  │  REST API /api/v1   embedded Vue UI │  │
  git push ─ webhook ►  │  │                                     │  │
                        │  │  ┌────────┐ ┌───────┐ ┌──────────┐  │  │
                        │  │  │ Podman │ │ Caddy │ │  Build   │  │  │
                        │  │  │ client │ │ admin │ │ pipeline │  │  │
                        │  │  └───┬────┘ └───┬───┘ └────┬─────┘  │  │
                        │  │      │     ┌────┴───┐      │        │  │
                        │  │  ┌───┴───┐ │ SQLite │ ┌────┴─────┐  │  │
                        │  └──┤       ├─┴────────┴─┤          ├──┘  │
                        │     ▼       ▼            ▼          ▼     │
                        │  podman.sock          caddy :2019 (admin) │
                        │  ┌─────┐ ┌─────┐ ┌─────┐     ▲            │
                        │  │app A│ │app B│ │ db  │ ◄───┘ :80/:443   │
                        │  └─────┘ └─────┘ └─────┘   reverse proxy  │
                        └───────────────────────────────────────────┘
```

### Components

| Component | Responsibility |
|---|---|
| **API server** | REST `/api/v1`, session + token auth, SSE streams for logs/events |
| **Dashboard** | Vue 3 + Nuxt UI SPA, embedded via `go:embed`, served by the API server |
| **Podman manager** | Talks to the Podman socket; creates containers, networks, volumes; generates Quadlet units on Linux; supervises directly on macOS |
| **Caddy manager** | Owns a managed Caddy process; renders desired state to Caddy JSON; pushes via admin API |
| **Build pipeline** | Turns deploy sources (tarball/git/image/compose) into tagged images and deployments |
| **Store** | SQLite (pure-Go driver); migrations embedded; every entity carries `server_id` for the future multi-server phase |
| **Plugin host** | Loads plugin manifests, dispatches lifecycle hooks, proxies plugin UIs/APIs |
| **CLI** | Same binary; talks to the API, so identical against local or remote servers |

### One binary, several roles

```
basepod server        # run the control plane (daemon)
basepod agent         # (phase 6) run as worker node agent
basepod login|deploy|apps|logs|...   # client commands
```

## Technology choices & rationale

- **Go 1.23+** — static cross-compiled binaries for all four platform
  targets; first-class Podman bindings (`github.com/containers/podman/v5`);
  same ecosystem as Podman and Caddy themselves.
- **SQLite via `modernc.org/sqlite`** — pure Go (no CGO) keeps
  cross-compilation trivial. WAL mode; one writer (the daemon).
- **Caddy admin API over Caddyfile** — atomic, validated, zero-downtime
  config swaps; no file templating races. Details in doc 04.
- **Quadlet over raw `podman run`** (Linux) — systemd owns restarts and
  boot-time startup; BasePod stays stateless about supervision. Details in
  doc 03.
- **Vue 3 + Vite + Nuxt UI** — user preference; Nuxt UI v3 works in plain
  Vue apps via its Vite plugin. Fallback if it fights us: PrimeVue.
  Details in doc 07.

## Repository layout

```
basepod/
├── cmd/basepod/            # main(); cobra commands (server, deploy, ...)
├── internal/
│   ├── api/                # HTTP handlers, middleware, SSE
│   ├── auth/               # sessions, tokens, RBAC
│   ├── build/              # build pipeline
│   ├── caddy/              # caddy process + admin API client
│   ├── podman/             # podman bindings wrapper, quadlet generation
│   ├── store/              # sqlite, migrations, repositories
│   ├── plugin/             # plugin host
│   └── server/             # wiring, config, lifecycle
├── web/                    # Vue 3 dashboard (Vite project)
├── docs/plan/              # these documents
├── install.sh              # curl-pipe installer
└── .github/workflows/      # CI + release
```

## Phases (summary — each has a detailed doc)

| Phase | Deliverable | Doc |
|---|---|---|
| 0 | Repo, CI, installer skeleton, `basepod server` boots | 02, 11 |
| 1 | Podman + Caddy managed, SQLite store, admin auth | 03, 04, 05 |
| 2 | Apps: deploy image/CLI tarball, domains, HTTPS, env, volumes, logs | 06 |
| 3 | Dashboard v1 | 07 |
| 4 | Teams, RBAC, tokens, audit | 08 |
| 5 | Git webhooks, compose, one-click templates | 06, 09 |
| 6 | Plugins | 09 |
| 7 | Multi-server agent mode | 10 |

Success criterion for v1.0: a newcomer can go from a fresh Ubuntu VPS or a
Mac to "my app is live on my domain with HTTPS" in under 10 minutes,
without reading more than the README.
